import React, { useState, useRef, useEffect } from 'react';
import { View, Text, Dimensions } from 'react-native';

const { width } = Dimensions.get('window');

const Tickets = () => {


    return (
        <View>
            <Text>Internet Speed Test</Text>
        </View>
    );
};

export default Tickets;
